apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skoro-ingress
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SKALA-10/SKoro-Infra.git
    targetRevision: main
    path: apps/skoro-ingress
  destination:
    server: https://kubernetes.default.svc
    namespace: sk-team-10
  syncPolicy:
    automated:
      prune: true
      selfHeal: true